#include <stdio.h>

/**
 * main - Starting Point
 * Return: 0 if successful and other otherwise
 */
int main(void)
{
	for (char c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
